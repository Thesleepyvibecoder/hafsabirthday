# Version 7

- Replaced the candle-in-image biryani asset with a plate-only biryani sticker asset.
- Added a separate animated HTML/CSS candle so the candle remains interactive.
- Removed the old `?preview=unlock` workflow.
- Local `index.html` opens directly into the birthday reveal for rapid editing/testing.
- Live hosting still respects the birthday date lock.
- `OPEN-WEBSITE.bat` now opens `index.html` directly and does not require Python.
