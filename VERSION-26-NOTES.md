# Version 26 Notes

- Final page message kept exactly as approved, but reduced substantially in size so the long uppercase message does not dominate the viewport.
- Secret sketch page now displays both supplied sketch photos.
- Both photos are inside the same visual border treatment used for the sketch placeholder.
- Desktop: sketches sit side by side.
- Mobile: sketches stack vertically.
- The existing sketch intro text was left unchanged.
- No other page content or behavior was intentionally changed.
